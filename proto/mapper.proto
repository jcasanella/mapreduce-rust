syntax = "proto3";

import "google/protobuf/empty.proto";

package mapper;

service Mapper {
    rpc GetNewTask (GetNewTaskRequest) returns (GetNewTaskResponse);
}

message GetNewTaskRequest {
    string worker_id = 1;
}

message GetNewTaskResponse {
    optional string file_path = 1;
    optional string task_id = 2;
}